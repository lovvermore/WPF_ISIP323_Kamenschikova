<Page x:Class="WPF_ISIP323_Kamenschikova.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPF_ISIP323_Kamenschikova.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Grid>
        <Grid.RowDefinitions>   
        </Grid.RowDefinitions>
        <ListBox x:Name="InfoList" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                
                <DataTemplate>
                    
                    <StackPanel x:Name="eblan">
                        <Image Source="{Binding Image}" Height="300" />
                        <TextBlock Text="{Binding Name}"/>
                        <TextBlock Text="{Binding Rating}"/>
                        <TextBlock Text="{Binding StartDate}"/>
                        <TextBlock Text="{Binding AgeCategory.Name}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
