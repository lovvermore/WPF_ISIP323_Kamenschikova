<Page x:Class="WPF_ISIP323_Kamenschikova.Pages.FilmPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPF_ISIP323_Kamenschikova.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="FilmPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>                    

        <ListBox x:Name="InfoList" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Grid.Row="1">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel>

                    </WrapPanel>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>                                <StackPanel x:Name="eblan">
                        <Image Source="{Binding Image}" Height="300" />
                        <TextBlock Text="{Binding Name}"/>
                        <TextBlock Text="{Binding Rating, StringFormat={}{0}/10}" />
                        <TextBlock Text="{Binding StartDate}"/>
                        <TextBlock Text="{Binding AgeCategory.Name}"/>
                        <TextBlock Text="{Binding Description}"/>
                        <Button x:Name="ChooseSession" Content="Выбрать сеанс"
            Tag="{Binding}" Click="ChooseSession_Click"/>
        </StackPanel>
                        <TextBlock Text="{Binding Date}"/>
                        <TextBlock Text="{Binding Hall.Name}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
